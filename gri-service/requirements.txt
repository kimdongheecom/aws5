# requirements.txt
# --- Web Service Backend (사용자 지정 버전 유지) ---
fastapi==0.110.0          # 👈 사용자 지정 버전으로 유지
uvicorn[standard]==0.27.1   # 👈 사용자 지정 버전으로 유지
pydantic==2.6.3            # 👈 사용자 지정 버전으로 유지
python-dotenv==1.0.1
requests==2.31.0
python-multipart==0.0.7

# --- Database (기존 설정 유지) ---
sqlalchemy==2.0.27
psycopg2-binary==2.9.9

# --- AI Model & GPU Inference (Llama3, RTX 5060, CUDA 12.1 호환) ---
transformers==4.42.3
accelerate==0.31.0
bitsandbytes==0.43.1
huggingface-hub==0.23.4  # 기존 0.23.3에서 호환성을 위해 소폭 상승

# --- Elasticsearch & Data Handling (신규 추가) ---
elasticsearch==8.14.0
pandas==2.2.2
openpyxl==3.1.2

# --- Tokenizer & Utilities (기존 설정 유지 및 호환성) ---
sentencepiece==0.2.0
protobuf==4.25.3
numpy==1.26.4